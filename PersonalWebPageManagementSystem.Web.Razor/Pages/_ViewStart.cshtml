@{

    Layout = "_Layout";
    //ViewData["Title"] = Model.FirstName + " " + Model.LastName;;
    var modelType = Model?.GetType().FullName ?? "null";
    ViewData["Title"] = "Model Type: " + modelType;
}
